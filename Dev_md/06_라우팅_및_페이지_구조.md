# 라우팅 및 페이지 구조

## React Router v7

react-router-dom v7을 사용하며, lazy loading(Suspense)과 보호된 라우트 패턴을 적용한다.

## 라우팅 설정

```jsx
// App.jsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import { Suspense, lazy } from 'react';
import AuthGuard from './components/common/AuthGuard';
import AdminGuard from './components/common/AdminGuard';

// Lazy loaded pages
const Home = lazy(() => import('./pages/Home'));
const IntroPage = lazy(() => import('./pages/IntroPage'));
const EduHubPage = lazy(() => import('./pages/EduHubPage'));
const Dashboard = lazy(() => import('./pages/Dashboard'));
const AIToolsPage = lazy(() => import('./pages/AIToolsPage'));
const SyllabusGenerator = lazy(() => import('./pages/tools/SyllabusGenerator'));
const RubricGenerator = lazy(() => import('./pages/tools/RubricGenerator'));
const AssignmentGenerator = lazy(() => import('./pages/tools/AssignmentGenerator'));
const FeedbackGenerator = lazy(() => import('./pages/tools/FeedbackGenerator'));
const EvaluatorGenerator = lazy(() => import('./pages/tools/EvaluatorGenerator'));
const Settings = lazy(() => import('./pages/Settings'));
const Login = lazy(() => import('./pages/auth/Login'));
const Register = lazy(() => import('./pages/auth/Register'));
const ForgotPassword = lazy(() => import('./pages/auth/ForgotPassword'));
const Board = lazy(() => import('./pages/community/Board'));
const BoardDetail = lazy(() => import('./pages/community/BoardDetail'));
const BoardWrite = lazy(() => import('./pages/community/BoardWrite'));
const Resources = lazy(() => import('./pages/Resources'));
const AIChecklist = lazy(() => import('./pages/AIChecklist'));
const PromptPractice = lazy(() => import('./pages/PromptPractice'));
const ApiGuidePage = lazy(() => import('./pages/ApiGuidePage'));
const AdminDashboard = lazy(() => import('./pages/admin/AdminDashboard'));
```

## 전체 라우트 맵

| 경로 | 컴포넌트 | 보호 | 설명 |
|------|----------|------|------|
| `/` | Home | - | 홈 페이지 |
| `/intro` | IntroPage | - | 소개 페이지 |
| `/edu-hub` | EduHubPage | - | 학습 허브 |
| `/dashboard` | Dashboard | AuthGuard | 개인 대시보드 |
| `/tools` | AIToolsPage | - | AI 도구 목록 |
| `/tools/syllabus` | SyllabusGenerator | AuthGuard | 강의계획서 생성기 |
| `/tools/rubric` | RubricGenerator | AuthGuard | 루브릭 생성기 |
| `/tools/assignment` | AssignmentGenerator | AuthGuard | 과제 생성기 |
| `/tools/feedback` | FeedbackGenerator | AuthGuard | 피드백 생성기 |
| `/tools/evaluator` | EvaluatorGenerator | AuthGuard | 학생평가 생성기 |
| `/settings` | Settings | AuthGuard | 설정 페이지 |
| `/login` | Login | - | 로그인 |
| `/register` | Register | - | 회원가입 |
| `/forgot-password` | ForgotPassword | - | 비밀번호 찾기 |
| `/community/board` | Board | - | 게시판 목록 |
| `/community/board/:id` | BoardDetail | - | 게시글 상세 |
| `/community/board/write` | BoardWrite | AuthGuard | 게시글 작성 |
| `/resources` | Resources | - | 학습 자료 |
| `/ai-checklist` | AIChecklist | - | AI 역량 체크리스트 |
| `/prompt-practice` | PromptPractice | - | 프롬프트 실습 |
| `/api-guide` | ApiGuidePage | AuthGuard | API 가이드 |
| `/admin/*` | AdminDashboard | AdminGuard | 관리자 대시보드 |

## 보호된 라우트

### AuthGuard

로그인한 사용자만 접근 가능한 라우트를 보호한다.

```jsx
// AuthGuard.jsx
import { Navigate, useLocation } from 'react-router-dom';
import { useAuth } from '../../contexts/AuthProvider';

export default function AuthGuard({ children }) {
  const { user, loading } = useAuth();
  const location = useLocation();

  if (loading) {
    return <div className="loading-spinner">Loading...</div>;
  }

  if (!user) {
    return <Navigate to="/login" state={{ from: location }} replace />;
  }

  return children;
}
```

### AdminGuard

관리자 권한이 있는 사용자만 접근 가능한 라우트를 보호한다.

```jsx
// AdminGuard.jsx
import { Navigate } from 'react-router-dom';
import { useAuth } from '../../contexts/AuthProvider';

export default function AdminGuard({ children }) {
  const { user, isAdmin, loading } = useAuth();

  if (loading) {
    return <div className="loading-spinner">Loading...</div>;
  }

  if (!user || !isAdmin) {
    return <Navigate to="/" replace />;
  }

  return children;
}
```

## 라우트 구조 (JSX)

```jsx
<BrowserRouter>
  <Suspense fallback={<LoadingSpinner />}>
    <Routes>
      {/* 공개 라우트 */}
      <Route path="/" element={<Home />} />
      <Route path="/intro" element={<IntroPage />} />
      <Route path="/edu-hub" element={<EduHubPage />} />
      <Route path="/tools" element={<AIToolsPage />} />
      <Route path="/login" element={<Login />} />
      <Route path="/register" element={<Register />} />
      <Route path="/forgot-password" element={<ForgotPassword />} />
      <Route path="/community/board" element={<Board />} />
      <Route path="/community/board/:id" element={<BoardDetail />} />
      <Route path="/resources" element={<Resources />} />
      <Route path="/ai-checklist" element={<AIChecklist />} />
      <Route path="/prompt-practice" element={<PromptPractice />} />

      {/* 인증 필요 라우트 */}
      <Route path="/dashboard" element={
        <AuthGuard><Dashboard /></AuthGuard>
      } />
      <Route path="/tools/syllabus" element={
        <AuthGuard><SyllabusGenerator /></AuthGuard>
      } />
      <Route path="/tools/rubric" element={
        <AuthGuard><RubricGenerator /></AuthGuard>
      } />
      <Route path="/tools/assignment" element={
        <AuthGuard><AssignmentGenerator /></AuthGuard>
      } />
      <Route path="/tools/feedback" element={
        <AuthGuard><FeedbackGenerator /></AuthGuard>
      } />
      <Route path="/tools/evaluator" element={
        <AuthGuard><EvaluatorGenerator /></AuthGuard>
      } />
      <Route path="/settings" element={
        <AuthGuard><Settings /></AuthGuard>
      } />
      <Route path="/community/board/write" element={
        <AuthGuard><BoardWrite /></AuthGuard>
      } />
      <Route path="/api-guide" element={
        <AuthGuard><ApiGuidePage /></AuthGuard>
      } />

      {/* 관리자 전용 라우트 */}
      <Route path="/admin/*" element={
        <AdminGuard><AdminDashboard /></AdminGuard>
      } />

      {/* 404 처리 */}
      <Route path="*" element={<Navigate to="/" replace />} />
    </Routes>
  </Suspense>
</BrowserRouter>
```

## 페이지 카테고리

### 공개 페이지 (인증 불필요)

- Home, IntroPage, EduHubPage
- AIToolsPage (목록만)
- Board, BoardDetail
- Resources, AIChecklist, PromptPractice
- Login, Register, ForgotPassword

### 보호된 페이지 (로그인 필요)

- Dashboard
- AI 도구 (Syllabus, Rubric, Assignment, Feedback, Evaluator)
- Settings
- BoardWrite
- ApiGuidePage

### 관리자 전용 페이지

- AdminDashboard (admin/* 경로)
